# Состояние проекта BS

Обновлять этот файл после каждой рабочей сессии: фиксировать выполненные изменения, проверки, ограничения и следующий актуальный шаг.

## 2026-09-23 — День 1: фон главной и бронирование

### Выполнено

- Десктопные фоновые слои главной проверены на ширинах 900, 1024, 1280, 1440, 1920 и 2560 px; мобильные фоны не изменялись.
- `bg-bottom` привязан к общей области FAQ и Яндекс-отзывов, сохраняет исходный размер и следует за изменением высоты раскрытого FAQ.
- На десктопе Яндекс-блок больше не закрывает рисунок `bg-bottom` сплошной заливкой.
- На странице домика добавлена нижняя CTA бронирования с `data-entry-point="4"` и явным `data-house-id`; такой же идентификатор добавлен основной кнопке блока бронирования.
- Устранено краткое закрытие и повторное открытие внешнего dialog при переключении порталов Контур.Отеля между датами заезда и выезда.
- Календарь не скрывается преждевременно при перерисовке поля и снова открывается после автоматического скрытия. В `core/tests_booking_layers.cjs` исправлена привязка сценария календаря к кнопке.
- `CLAUDE.md` больше не требует отсутствующий `.ai/backgrounds-handoff.md`; после каждой сессии актуальная запись ведётся здесь.

### Проверки

- `manage.py check` — успешно.
- Точечные Django-тесты — 24/24.
- Полный набор Django-тестов — 103/103.
- `core/tests_kontur.cjs` — 17/17.
- Portal/layer-сценарии (первый и повторный выбор дат, смена порталов, фокус, десктоп и 390 px), раскрытие FAQ, DOM и горизонтальный overflow — успешно.
- Локальная нижняя CTA: до клика скрипта Контура нет; после клика передаются дом №25 и источник `4`; при закрытии возвращается фокус. Через 5 секунд корректно показывается fallback. Служебное уведомление при этом было перехвачено тестом и владельцу не отправлялось.
- `git diff --check` — успешно.

### Ограничения

- Production `https://best-season.online/` и запасной адрес Amvera `https://best-season-sfnvsd24.amvera.io/` возвращали `503 Service Unavailable` при повторной проверке 23 сентября.
- Локально реальный виджет Контур.Отеля не завершил инициализацию за 5 секунд; корректно показан fallback. Живой выбор дат и гостей у провайдера не подтверждён.
- Commit, push и deploy не выполнялись.

### Следующий шаг

- Получить рабочий адрес или восстановить сайт, затем повторить read-only проверку реального виджета Контур.Отеля без отправки брони или формы.

### Дополнительная локальная проверка

- В каталоге домиков нижняя CTA с источником `5` есть в шаблоне, но вся секция скрыта правилом `.page-houses .section--deep { display: none; }`. Нужно решить по утверждённому десктопному макету, показывать её или убрать неиспользуемую разметку; без этого менять внешний вид не следует.
- Локальные проверки слоёв бронирования прошли на тестовом макете порталов. Поведение живого виджета Контур.Отеля остаётся неподтверждённым; дальнейшие правки его DOM без рабочего экземпляра будут предположительными.

### Визуальный предпросмотр

- Сняты текущие десктопные кадры главной (1440 CSS px), фона FAQ/Яндекс-отзывов с закрытым и раскрытым вопросом, страницы первого домика и нижней CTA. Файлы сохранены в папке визуализаций текущей задачи Codex, вне репозитория.
- Уже запущенные локальные серверы на 8000/8001 отдавали старый шаблон без нижней CTA. Для кадров использован временный сервер на 8002 с текущим кодом; после съёмки он остановлен.
- В локальном предпросмотре фотографии отображались заглушками, а внешний Яндекс-виджет не загрузился. Пустое окно Контур.Отеля не выдавать за подтверждённый интерфейс провайдера; заявку и бронь не отправляли.

### Уточнение фонов по замечанию пользователя

- На эталоне `Desktop Main.png` (1440 px) повторно сверены контуры декора. На десктопе исправлены координаты `bg-1` у «О нас», `bg-8` у «Интересное рядом»/фотогалереи и `bg-bottom` у FAQ. Мобильные правила не менялись.
- Локальная проверка в браузере подтвердила новые координаты изображений на ширине 1440 px. Остальные слои по этому замечанию не переставлялись: для точной оценки полного совпадения нужен снимок с реальными фотографиями и указание участков, которые пользователь считает неверными.

## 2026-09-24 — Десктопные фоны галереи и FAQ

- По сравнению с `Desktop Main.png` сдвинуты фоновые слои `bg-6` и `bg-7`: они теперь обрамляют переход фотогалереи к FAQ, оставляя середину с заголовком свободной. Координаты заданы относительно секции галереи; мобильные правила не менялись.
- К ссылке на `main.css` добавлена версия для обновления кеша. Уже запущенные серверы на 8000/8001 продолжают отдавать закешированный в их процессах HTML-шаблон; после перезапуска сервера новая ссылка появится на странице. На 8001 обновлённый CSS проверен в браузере при 1440 CSS px.
- Проверено отсутствие горизонтального переполнения на 1024, 1440 и 1920 CSS px. Полного пиксельного совпадения с макетом пока не заявлять: в локальном просмотре фотографии — заглушки, а дополнительный блок отзывов отсутствует в эталонном PNG.

## 2026-09-24 — Ответ Контур.Отеля по сценарию бронирования

- Поддержка описала обычный путь гостя в HotelWidget v2: даты и гости → доступные номера → тариф → контактные данные → «Бронировать без предоплаты». Бронь попадает в «Не выбран номер», источник — «Сайт-отеля».
- Ответ не содержит API/параметров для предвыбора дома, дат или гостей. Код вставки поддержка направила смотреть в «Сайт» → «Модули для вставки на внешние сайты» → «Код для вставки на сайт»; ранее присланный код содержит `HotelWidget.init/add`, но не параметры предвыбора.
- Из скриншотов заказчика известны точные названия четырёх домов в PMS; технические идентификаторы объектов из них не следуют.
- Код в этой сессии не менялся; проверок не запускалось. Следующий шаг — запросить у поддержки явный ответ о предвыборе и уточнить режим предоплаты, поскольку описанное «без предоплаты» расходится с формулировкой ТЗ о предоплате.
- Проверка открытых материалов Контура: страница `https://kontur.ru/hotel/modul-bronirovaniya` прямо указывает на возможность настроить предоплату, полную оплату или оплату при заселении; опубликованные материалы `https://kontur.ru/hotel/features/direct-sales` и `https://kontur.ru/hotel/spravka/626-vidget_bronirovaniya` описывают ручной выбор гостем дат/категории/тарифа, но не документируют параметры предвыбора для HotelWidget v2. Отсутствие публичного описания не доказывает отсутствие API. Следующий шаг — точный вопрос техподдержке о параметрах предвыбора и настройках обязательной предоплаты для этого аккаунта.

## 2026-09-24 — Текущий статус

- Рабочая ветка `main` совпадает по коммиту `147d407` с `origin/main` и `amvera/master`; поверх него остаются незакоммиченные изменения календарного слоя Контур.Отеля и его точечного теста.
- В этой сессии выполнена только проверка состояния; код и выкладка не менялись. Актуальный блокер — подтверждение живого сценария Контур.Отеля и доступность production.

## 2026-09-24 — Эталон главной в проекте

- Копия присланного макета `Desktop Main (6).png` сохранена в `design/reference/Desktop Main.png` (1440×9060). SHA-256 копии совпадает с исходным файлом; последующие сравнения брать из этого пути.
- Код не менялся, тесты не запускались. Следующий шаг по фону — измерить вертикальный сдвиг `bg-1` относительно этого эталона и текущего снимка страницы; по календарю требуется отдельно воспроизвести живой сбой.

## 2026-09-24 — Сверка присланных снимков

- Сравнены `design/reference/Desktop Main.png` (1440×9060) и `C:\Users\}I{YK\Desktop\best-season-sfnvsd24.amvera.io.png` (2880×16384). Снимок сайта уменьшен вдвое и выровнен по верхней границе секции «О нас» (примерно 2222 и 860 px соответственно).
- Верхний левый контур первого фона на макете находится ориентировочно на 20 px ниже, чем на снимке сайта, относительно секции «О нас». Это оценка по доступным растровым кадрам, не финальный замер для CSS.
- На снимке сайта первый экран значительно выше; шапка во время захвата находится поверх «О нас». Фото домиков и часть карточек территории/окрестностей отличаются от макета или показаны заглушками. Снимок обрывается до фотогалереи и FAQ, поэтому эти секции не сверены.
- Изменений кода и новых тестов нет. Следующий шаг при разрешении на правку — сдвинуть `bg-1` вниз на измеренную величину и проверить новый снимок при идентичном viewport; сбой календаря остается отдельной задачей.

## 2026-09-24 — Сводка изменений и актуальные цели

### Что сделано за 23–24 сентября

- Главная: восстановлены и скорректированы десктопные фоновые слои. `bg-bottom` привязан к FAQ и Яндекс-отзывам; `bg-1`, `bg-8`, `bg-6`, `bg-7` переставлены по сравнению с макетом. Мобильные правила не менялись. Для `main.css` добавлена версия в ссылку, чтобы обновить кеш.
- Бронирование: на странице домика добавлена нижняя CTA с явным домом и источником перехода; исправлены локально воспроизведённые переключения порталов Контур.Отеля, повторное открытие календаря и передача состояния. Изменения вошли в коммиты `8357a4d` и `147d407`.
- Поверх коммита `147d407` локально изменены `config/static/js/booking-layers.js` и `core/tests_booking_layers.cjs`: внутренний `PopupContent` календаря получает вертикальную прокрутку, тест проверяет достижимость нижней строки. Эти правки ещё не закоммичены; сообщение пользователя о продолжающемся сбое календаря имеет приоритет над успешными тестами.
- Присланный эталон сохранён в `design/reference/Desktop Main.png`; файл ещё не закоммичен. Снимок сайта хранится вне проекта: `C:\Users\}I{YK\Desktop\best-season-sfnvsd24.amvera.io.png`.

### Проверено и не подтверждено

- Ранее прошли `manage.py check`, Django 103/103, Kontur 17/17 и локальные сценарии слоёв; на проверенных ширинах не было горизонтального переполнения. Это подтверждает локальный код и тестовые сценарии, но не исправность живого календаря провайдера.
- При сравнении двух присланных PNG верхний левый контур `bg-1` на макете оказался ориентировочно на 20 px ниже относительно начала «О нас». Снимок сайта имеет 2880×16384 px, эталон — 1440×9060 px; первый экран разной высоты, а снимок сайта обрывается до галереи и FAQ. Точная CSS-поправка и полное визуальное совпадение пока не подтверждены.
- Последняя зафиксированная проверка production 23 сентября вернула 503; доступность сейчас не перепроверялась. Живые выбор дат и гостей, бронирование, доставка fallback-заявки и обязательная предоплата не подтверждены.

### Цели по порядку

1. Воспроизвести на работающем экземпляре Контур.Отеля сбой при выборе дат и закрытии календаря; определить фактическую последовательность событий и исправить её без предположений о DOM провайдера. Повторить выбор и закрытие на десктопе и мобильной ширине, проверить последнюю строку календаря.
2. При одинаковой ширине и состоянии страницы уточнить сдвиг `bg-1` по эталону, затем проверить 1440 px и согласованные адаптивные ширины. Сверить галерею и FAQ отдельным полным кадром, поскольку присланный снимок их не содержит.
3. Дождаться уточнения пользователя о предоплате; получить от поддержки Контура явный ответ о предвыборе дома/дат/гостей и технических идентификаторах. Не считать описание обычного сценария HotelWidget v2 подтверждением этих возможностей.
4. После восстановления доступности production выполнить read-only проверку живого виджета и fallback без отправки реальной брони или формы. Отдельно сверить готовность страниц, юридических текстов/согласий и аналитики перед решением о выкладке.

Коммит, push и deploy для текущих незакоммиченных файлов не выполнялись. В рабочей папке также есть сторонний `.claude/settings.local.json`; его не включать автоматически в коммиты.

## 2026-09-24 — Фон главной заново из Desktop Main.svg

- Старые слои `bg-2…8`, `bg-bottom` (PNG, 6,3 МБ) и подгонка координат по PNG-эталону удалены. Вместо них 8 SVG `config/static/img/home-backgrounds/fon-*.svg`, вырезанных из экспорта фрейма `Desktop Main.svg` без перерисовки: viewBox каждого файла — рамка слоя в координатах макета, размер 1:1 с Figma.
- С масок сняты только края, совпадающие с краем фрейма (x = 0 / 1440): на экранах шире 1440 линии продолжаются в боковые поля. Обрезки масок внутри фрейма оставлены как в макете.
- Привязка: каждый слой лежит в своей секции, `left` от центрированного фрейма 1440, `top` = смещение от опорного элемента секции по макету. Таблица опорных элементов — в комментарии «Фон главной» в `main.css`. Нижний слой перенесён в подвал (`includes/footer.html`, показывается только на `.page-home`), потому что блока Яндекс-отзывов в макете нет.
- Мобильная версия (<900 px) не менялась: там по-прежнему `bg-1.png` в «О нас», теперь с `loading="lazy"`, на десктопе не скачивается.
- Проверки: при 1440 вертикальное смещение каждого слоя от опорного элемента совпало с макетом до 0,01 px, горизонталь — точно. Сами слои сверены с макетом растром: расхождение альфы в пределах сглаживания. Горизонтальной прокрутки нет на 390/768/900/1024/1280/1440/1920/2560. На <900 fon-*.svg не запрашиваются. Django-тесты 103/103.
- Ограничения: на заглушках фото линии просвечивают сквозь полупрозрачную штриховку — на настоящих фото этого не будет. На ширинах >1440 в боковых полях виден прямой срез нижней маски слоя у слогана и верхней у домиков — так они обрезаны в самом макете.
- Следующий шаг: при необходимости включить gzip статики (`whitenoise.storage.CompressedManifestStaticFilesStorage`) — SVG сжимаются с 4,1 МБ до ~1,6 МБ.

## 2026-09-24 — Фон главной: без масок и без обрывков

- По замечанию «обрывки линий»: слои заменены полными рисунками из того же `Desktop Main.svg` — маски Figma сняты целиком. Срезы масок, в макете спрятанные под фото и краем фрейма, на сайте проступали прямыми обрывами, потому что блоки стоят не пиксель в пиксель как в макете. Координаты — те же, из макета; изменились только рамки файлов, размеры в CSS и шаблонах обновлены.
- Главный обрыв давала секция территории: `.section--pattern` от старой заглушки узора имела `overflow: hidden; isolation: isolate` и резала слой территории по своему верху и низу. На главной (от 900 px) это снято.
- Проверки: смещения слоёв от опорных элементов при 1440 совпадают с макетом; горизонтальной прокрутки нет на 1024/1440/1920; Django 103/103. Рендер Chromium слоя с маской и без маски одинаков по резкости — если «мыло» останется, нужен скрин конкретного места с масштабом экрана.

## 2026-09-24 — Фон главной: «пунктирные» тонкие линии

- Причина потери качества: большая часть линий в макете — контуры шириной 0,07–0,09 px (кольца из двух почти совпадающих кривых) с заливкой 30%. Chrome (Skia) сглаживает такие сверхтонкие заливки выборкой, и линия распадается на точки/штрихи. В Figma и при растеризации через cairo они непрерывные.
- Исправление в самих SVG: такие кольца заменены обводкой по той же кривой шириной 0,6 px с прозрачностью, пересчитанной так, чтобы количество «краски» осталось прежним (прозрачность × ширина, +12% поправка на сглаживание обводки). Толстые линии (≥0,6 px) не тронуты. Геометрия и координаты прежние, сдвиг не больше 0,3 px.
- Проверка: рендер Chromium каждого слоя сравнен с эталонной растеризацией cairo — структурное расхождение уменьшилось, точечного распада нет (снимок при 2000 px и масштабе 1,15). Вес восьми файлов упал с 4,4 до 2,6 МБ.

## 2026-09-25 — Фон «Размещения» из Houses.svg

- Три слоя фона в выгрузке `Houses.svg` (фрейм 1440×4282) оказались теми же рисунками, что на главной, сдвинутыми на целое число пикселей: `fon-about` (x 512, y −696), `fon-slogan` (x −666, y 1431), `fon-footer` (x 221, y 3998). Новых SVG нет, файлы берутся из `img/home-backgrounds/` (после главной уже в кеше). Маски сняты, как на главной.
- Привязка: `fon-about` — к секции каталога (её верх = верх фрейма), `fon-slogan` — к секции доп услуг по первой карточке (1900.91), `fon-footer` — к подвалу по иконкам соцсетей (4008.39). Общий контекст наложения — `#main` (`isolation: isolate; overflow: clip`), чтобы слой доп услуг, заходящий на ряд домиков, лежал под карточками. Стили — блок «Фон „Размещения“» в `main.css`; версия `main.css` в `base.html` обновлена.
- Проверки: при 1440 смещения слоёв от опорных элементов совпали с макетом до 0,01 px; горизонтальной прокрутки нет на 390/900/1024/1440/1920/2560; на 390 SVG не запрашиваются; слои главной не сдвинулись; Django 103/103.
- Замечено, не исправлялось: блок FAQ на сайте на 40 px выше макета (первая плашка 2990.9 против 3030.91) — слой подвала от этого не зависит.
- Следующий шаг: SVG остальных страниц (по одному на фрейм).

## 2026-09-24 — Порядок завершения бронирования

- Зафиксировано разделение работ: фоновые правки ведутся отдельно; задача бронирования охватывает состояние формы, HotelWidget v2, fallback, визуальные слои, предоплату и финальную production-проверку.
- От владельца нужны доступ/скриншоты настроек тарифа и оплаты в Контур.Отеле, технический ответ поддержки по API предзаполнения и согласование одной тестовой брони только на финальном этапе.
- До отдельного разрешения не отправлять реальные заявки, уведомления и платежи. Сначала локальные тесты и read-only проверка production, затем контролируемый сквозной тест.
- По присланным снимкам: четыре категории домов, номера, тарифы и продажа на сайте настроены; онлайн-платежи не подключены. В кабинете доступны варианты Яндекс Пэй Сплит, СБП и ЮKassa. До подключения одного из них нельзя подтвердить сценарий обязательной предоплаты и автоматического подтверждения после оплаты. Ответ поддержки по API предзаполнения HotelWidget v2 ожидается.

## 2026-09-25 — Фон «Акций» из Special Offers.svg

- Выгрузка взята из «Загрузок» (в чат не загружалась). Четыре слоя: `fon-about` (x 359, y −704) и `fon-footer` (x 221, y 3028) — рисунки главной со сдвигом; два своих вырезаны в `fon-offers-left.svg` (x −400, y 400) и `fon-offers-right.svg` (x 1063, y 1282) тем же способом, что fon-* главной (без масок — внутри фрейма они ничего не прятали; кольца тоньше 0,6 px → обводка 0,6 px). Метод проверен: слой «Размещения» им же собирается в рисунок, совпадающий с `fon-slogan.svg` по растру.
- Контекст наложения — `#main`, как на «Размещении». Стили — блок «Фон „Акций“» в `main.css`.
- Сверка с макетом, исправлено (от 900 px): заголовок и карточки ниже на 1 px (226 вместо 225); вопросы — сверху 130, заголовок 95, под ним 50 (было 120/90/60); подвал — поле 100 вместо 60, от последней плашки до знака 160, как в макете. Ниже 900 прежние значения.
- Проверки: при 1440 все координаты совпадают с макетом (заголовок 226, карточки 381/945/1531, вопросы 2201/2346, слои до 0,01 px); горизонтальной прокрутки нет на 390/900/1024/1440/1920/2560; на 390 SVG не запрашиваются; Django 103/103.
- Открытый вопрос (наполнение, не вёрстка): в макете на «Акциях» свои 4 вопроса (скидка в праздники, суммирование скидок, другие сайты, сертификаты), а страница выводит общие вопросы главной (`show_on_home`). Нужны ответы на эти вопросы и решение, как их отбирать.

## 2026-09-24 — Клики и прокрутка календаря бронирования

- Исправлена доступность нижних дат: прокрутка включается не только у корня `DateRangePicker`, но и у внутреннего `PopupContent`; подгонка теперь работает и на ширине меньше 768 px.
- Браузерная fixture-проверка на 1440×900 и 390×844 подтвердила клики по датам, доступность последней строки, автозакрытие и повторное открытие календаря, а также закрытие и повторное открытие внешнего окна. JS syntax, 24 профильных теста и `git diff --check` прошли.
- Живая проверка заблокирована: `https://best-season.online/` и Amvera-адрес отвечают 503. До восстановления production результат нельзя считать подтверждённым на реальном Контур.Отеле.

## 2026-09-25 — Фон страницы домика из The First…The Fourth.svg

- Выгрузки взяты из «Загрузок». У всех четырёх домиков фон одинаковый (рисунки совпадают по растру) — один набор на шаблон `house_page.html`. Четыре слоя, своих рисунков, в файлы главной не входят: `fon-house-top` (x −255, y −510), `fon-house-gallery` (x 489, y 947), `fon-house-left` (x −666, y 1497), `fon-house-footer` (x 211, y 3139). Вырезаны тем же способом, что остальные fon-* (маски сняты, кольца тоньше 0,6 px → обводка 0,6 px).
- Привязка: поле дат (354), большое фото мозаики (1019.55), карточка «Вместимость» (1444.75), иконки соцсетей в подвале (3149.71, на 318 от верха подвала). Контекст наложения — `#main`. Нижний слой лежит в общем подвале рядом с `fon-footer`; на странице домика показывается свой, на остальных он скрыт и не скачивается. Стили — блок «Фон страницы домика» в `main.css`.
- Проверки: при 1440 и 1920 смещения слоёв от опорных элементов совпадают с макетом до 0,01 px; горизонтальной прокрутки нет на 390/1024/1440/1920; на 390 SVG не запрашиваются; на главной файлы домика не запрашиваются; Django 103/103.
- Замечено: на проде блок бронирования был выше макета на 128 px — исправлено следующей записью. Тёмной полосы «Выбрали домик?» в макете нет — на сайте она закрывает часть левого слоя.

## 2026-09-25 — Блок бронирования домика по макету, пояснение в подсказке

- Абзац «Предварительный расчёт. Календарь не показывает занятость домов…» (добавлен 13–14.09, в макете его нет) убран из правой колонки и перенесён в подсказку по значку «i» справа от цены. Подсказка открывается наведением, фокусом с клавиатуры и касанием; Esc закрывает (WCAG 1.4.13), текст связан с кнопкой через `aria-describedby`. Показ — CSS, в `calendar.js` только закрытие по Esc.
- `[data-total]` перенесён на внутренний `<span>` цены: `calendar.js` перезаписывает `textContent` и иначе стирал бы значок.
- Правая колонка выставлена по макету The First (от 700 px): карточка гостей 339.3 (подписи под счётчиками 18/18 вместо 18/22 — строки гостей шли на 4 px реже), строка «ночи/гости» с 802, цена с 845, кнопка 887.92–959.39 — низ кнопки на линии низа календаря. Блок бронирования стал 666 px вместо 793, мозаика начинается на 1020 (в макете 1019.55).
- Проверки: при 1440 строки колонки совпадают с макетом до 2 px; пересчёт цены после выбора дат значок не стирает; на 390 подсказка в пределах экрана, горизонтальной прокрутки нет; `tests_calendar.cjs` 5/5, `tests_kontur.cjs` 19/19, Django 103/103. `tests_booking_layers.cjs` в песочнице не запускается (к правке не относится).
- Тёмная полоса «Выбрали домик?» (точка входа №4 по п. 5.1 ТЗ) не трогалась — ждёт решения.

## 2026-09-25 — Живая проверка календаря Контура после выкладки: две ошибки

Проверено на best-season-sfnvsd24.amvera.io (страница домика, 1440):
- **Календарь висел над шапкой.** Контур сам двигает всплывающий календарь вместе с полем дат при прокрутке окна бронирования; наш `fitRangePickers` прижимал его к верху (`top ≥ 8`), и когда поле уезжало вверх, календарь оставался висеть над шапкой. Прижим к верху убран, остался только подъём у нижнего края.
- **Календарь открывался снова после выбора дат.** Когда Контур закрывает календарь, мы возвращали фокус в поле даты, а Контур на фокус открывает календарь заново (в логе фокусов — чередование `sync`/выход из режима и `r.focus` виджета). Теперь после закрытия календаря фокус уходит на кнопку «Проверить наличие» той же формы. Добавлен сценарий в `tests_booking_layers.cjs`; на старом коде он падает, на новом проходит.
- Убрана тёмная полоса «Выбрали домик?» на странице домика (в макете её нет). П. 4.1.8 ТЗ закрывают блок бронирования вверху (точка входа 3) и липкая кнопка (точка входа 6).
- Проверки: фикстура слоёв 42/42 на 1440 и 390, `tests_calendar.cjs` 9/9, `tests_kontur.cjs` 19/19, Django 103/103.
- Замечено: при первом открытии окна в фоновой вкладке браузера виджет ушёл в резервную форму («временно недоступно»), по «Повторить загрузку» загрузился — это проверка видимости контейнера при скрытой вкладке, не ошибка на обычном экране.

## 2026-09-25 — Визуальный аудит десктопа по Desktop.svg/PNG

- Создан `VISUAL_AUDIT_DESKTOP.md`: 179 расхождений на 7 фреймах (6 Critical, 136 Major, 37 Minor, 0 Micro). Сверены SVG-геометрия, полные скриншоты production при 1440×900/DPR=1, DOM, шрифты и наложения; первый слайд hero зафиксирован.
- Код, данные, зависимости, Git и деплой в этой сессии не менялись. Текущие незакоммиченные изменения других файлов не затрагивались.
- Ограничения: у прочих дочерних страниц нет отдельного фрейма в исходных файлах; динамические состояния календаря/Контур-виджета и фактические характеристики домов требуют отдельной проверки. Следующий шаг — согласовать спорные данные/состояния и исправлять пункты в порядке зависимостей отчёта.

## 2026-09-25 — Виджет Контура оформлен по макетам «Pop-up Бронь 1–3 шаг»

- Блок «Оформление виджета Контура» в конце `kontur-booking.css`. Основа — подмена токенов Контура `--k-color-*` внутри окна и порталов, плюс переменных темы `--bm-theme-*` на `body[data-modal-open]` (SDK пишет их в `style` у body, поэтому стоят с !important). Замена токенов перекрашивает и экраны, до которых сейчас не дойти: выбор тарифа, данные гостя, оплату.
- Что получилось:
  - основные кнопки — терракотовые пилюли, остальные — плашка #E6DFD1;
  - поля дат и гостей — бежевые плашки, строка поиска лежит на кремовом фоне;
  - домики и баня/беседки — отдельные белые карточки со скруглением 20;
  - дни календаря свободны на #FFF0E4;
  - окно Контура «Бронирование» — кремовое, скругление 40, заголовок Etude Noire оливковым, крестик плашкой 50×50, как у нашего окна;
  - шрифт — Montserrat.
- Основную кнопку от второстепенной отличает `data-bs-kind`, его ставит `kontur.js` (`watchButtonKinds`). Пока окно открыто, MutationObserver смотрит на исходный фон кнопки. Выключенные кнопки не помечаются, иначе «Забронировать» осталась бы серой.
- Хрупкое место: карточки списков держатся на именах CSS-модулей виджета (`.ZAbtf8`, `.ZSaP3T`, `.VwZTwv`). Если Контур пересоберёт виджет, откатится только раскладка карточек, цвета останутся.
- Не проверено вживую: шаги тарифа, данных гостя и оплаты. Виджет на все даты отвечает «Мы не нашли свободных номеров», и онлайн-оплата в Контуре не подключена — это настройки на стороне Софии.
- Проверки:
  - снимки живого виджета перерисованы локально при 1440 и 390;
  - `tests_kontur.cjs` 20/20, добавлен сценарий на `data-bs-kind`;
  - `tests_calendar.cjs` проходит, Django проходит.
